name: Prek checks
on:
  push:
    branches:
    - dev
  pull_request:
    branches:
    - dev

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: j178/prek-action@v2
